---
title: useSigmaGraph
description: 把 graphology 的变更桥接成 Vue 的响应式信号，提供 version、order、size 与变更回调。
category: base
seo:
  title: useSigmaGraph
  description: Bridge graphology mutations into Vue reactivity, exposing a version counter, node and edge counts, and an update callback.
---

## 用法

graphology 的 `Graph` 是纯可变对象，Vue 的响应式系统抓不到它的变更——依赖图状态的 UI（图例、统计、检索结果）不会自动更新。这个 composable 订阅图事件并递增 `version`，让下游用常规的 `computed` / `watch` 就能响应：

:component-example{name="UseSigmaGraphExample" client-only="true" class="example-stage"}

```vue [UseSigmaGraphPanel.vue]
<script setup lang="ts">
const { graph, version, order, size, onGraphUpdate } = useSigmaGraph()

// 自己的派生值：读一次 version 建立依赖
const hubs = computed(() => {
  void version.value
  return graph.value.nodes().filter(key => graph.value.degree(key) > 5)
})
</script>
```

`void version.value` 这行不是摆设——`graph.value.nodes()` 本身不是响应式的，不读 `version` 就建立不起依赖，计算属性永远不会重算。库内的 `order` 与 `size` 也是这么写的。

### 监听的事件

九个 graphology 事件会触发递增：`nodeAdded`、`nodeDropped`、`nodeAttributesUpdated`、`edgeAdded`、`edgeDropped`、`edgeAttributesUpdated`、`cleared`、`edgesCleared`、`attributesUpdated`。

::warning
**每个事件都递增一次，没有防抖。** 批量导入一千个节点会触发一千次 `version` 变化与一千次 `onGraphUpdate` 回调。这类操作请自己做批处理，或者用 `applyGraphDiff()` 一次性同步。
::

`version` 从 `1` 起步而不是 `0`：初始 watch 是 `immediate` 的，挂上监听后会先递增一次。不要把 `version === 0` 当作「还没初始化」的判据。

### 变更回调不返回退订函数

`onGraphUpdate(callback)` 返回 `void`。回调随当前作用域自动注销，不需要也拿不到手动退订的手柄：

```ts
const { onGraphUpdate } = useSigmaGraph()

onGraphUpdate(() => {
  // 组件卸载时自动移除
})
```

### 脱离子树使用

不传参数时取当前 `SigmaGraph` 上下文里的图，因此必须在子树内调用。传入 `source` 就不依赖上下文了——任何 graphology 实例都能桥接：

```ts
const graph = shallowRef(new Graph())
const { order, size } = useSigmaGraph(graph)
```

图实例被替换时监听会自动迁移到新实例上。

## API

### useSigmaGraph()

`useSigmaGraph(source?: MaybeRefOrGetter<Graph>): UseSigmaGraphReturn`{lang="ts-type"}

#### Parameters

::field-group
  :::field{name="source" type="MaybeRefOrGetter<Graph>"}
  要桥接的 graphology 实例。省略则取当前 `SigmaGraph` 上下文中的图，此时必须在其子树内调用。
  :::
::

#### Returns

::field-group
  :::field{name="graph" type="Readonly<Ref<Graph>>"}
  原生 graphology 实例。
  :::

  :::field{name="version" type="Readonly<Ref<number>>"}
  图每次变更后递增。用于让计算属性重算，或作为缓存键。初始值为 `1`。
  :::

  :::field{name="order" type="Readonly<Ref<number>>"}
  节点数。
  :::

  :::field{name="size" type="Readonly<Ref<number>>"}
  边数。
  :::

  :::field{name="onGraphUpdate()" type="(callback: () => void) => void"}
  图变更时触发的回调，随作用域自动注销。返回 `void`，不是退订函数。
  :::
::

类型从根出口取：

```ts
import type { UseSigmaGraphReturn } from '@movk/sigma'
```
