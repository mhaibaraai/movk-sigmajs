---
title: useSigmaCamera
description: 相机操作。缩放、复位、聚焦节点、容纳全图，以及图坐标到屏幕坐标的换算。
category: interaction
seo:
  title: useSigmaCamera
  description: Control the sigma camera — zoom, reset, focus a node, fit the viewport to nodes, and convert graph coordinates to screen coordinates.
---

## 用法

`useSigmaCamera()` 全部基于原生 `sigma.getCamera()`，需要更底层的控制随时可以直接拿实例自己调。

`useSigma()` 是 inject，因此这个 composable 必须在 `SigmaGraph` 子树内调用。示例由渲染图的外壳与消费上下文的面板两个文件组成——这也正是真实应用的结构：

:component-example{name="UseSigmaCameraExample" client-only="true" class="example-stage"}

```vue [UseSigmaCameraPanel.vue]
<script setup lang="ts">
const { zoomIn, zoomOut, reset, gotoNode, fitTo } = useSigmaCamera()
</script>

<template>
  <button type="button" @click="gotoNode('n3', { ratio: 0.4 })">
    聚焦 n3
  </button>
</template>
```

所有动画方法返回 `Promise`，内部先 `await whenReady()`，因此在实例就绪前调用也不会丢失——会排队等到实例创建完成。

::warning
`fitTo()` 依赖可选 peer `@sigma/utils`。未安装时抛出的错误里带有安装命令，捕获后直接展示给用户即可。
::

### 两套坐标系

`toViewport()` 换算的是**原始图坐标**。节点位置不要走这里：

```ts
// 节点位置——getNodeDisplayData 返回的是 sigma 归一化后的 framed 坐标
const screen = sigma.framedGraphToViewport(sigma.getNodeDisplayData(key))

// 原始图坐标
const { toViewport } = useSigmaCamera()
const screen = toViewport({ x: 0, y: 0 })
```

混用会让覆盖层整体错位。`gotoNode()` 内部把显示坐标直接交给相机而不做换算，是因为相机的 `x` / `y` 恰好也在 framed 坐标系——与覆盖层的换算是两件事。

## API

### useSigmaCamera()

`useSigmaCamera(): UseSigmaCameraReturn`{lang="ts-type"}

#### Returns

::field-group
  :::field{name="zoomIn()" type="(options?: Partial<AnimateOptions> & { factor?: number }) => Promise<void>"}
  放大，`factor` 为缩放倍数。
  :::

  :::field{name="zoomOut()" type="(options?: Partial<AnimateOptions> & { factor?: number }) => Promise<void>"}
  缩小，`factor` 为缩放倍数。
  :::

  :::field{name="reset()" type="(options?: Partial<AnimateOptions>) => Promise<void>"}
  复位到初始视角。
  :::

  :::field{name="goto()" type="(state: Partial<CameraState>, options?: Partial<AnimateOptions>) => Promise<void>"}
  移动到指定相机状态。
  :::

  :::field{name="gotoNode()" type="(key: string, options?: Partial<AnimateOptions> & { ratio?: number }) => Promise<void>"}
  移动到指定节点。节点不存在或尚未渲染时不动。
  :::

  :::field{name="fitTo()" type="(nodes?: string[], options?: { animate?: boolean }) => Promise<void>"}
  调整视口容纳指定节点，省略则容纳全图。需要可选 peer `@sigma/utils`。
  :::

  :::field{name="getState()" type="() => CameraState | null"}
  当前相机状态，未就绪时为 `null`。
  :::

  :::field{name="toViewport()" type="(point: Coordinates) => Coordinates | null"}
  原始图坐标转屏幕坐标，未就绪时为 `null`。节点位置请改用 `sigma.framedGraphToViewport()`。
  :::
::

`AnimateOptions` 来自 `sigma/utils`，`CameraState` 与 `Coordinates` 来自 `sigma/types`。本库不 re-export 上游类型，需要时从原包直接 import；本库自己的类型从根出口取：

```ts
import type { UseSigmaCameraReturn } from '@movk/sigma'
```
