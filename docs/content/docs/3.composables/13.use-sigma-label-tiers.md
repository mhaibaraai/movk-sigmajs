---
title: useSigmaLabelTiers
description: 按相机比例分级显示标签，视野越广只留下越重要的那几档。
category: interaction
seo:
  title: useSigmaLabelTiers
  description: Show fewer labels as the camera zooms out, keeping only the top tiers by relaying the current level through a style rule.
---

## 用法

拉远之后节点变密，标签再全出就只剩噪声。档位先由 [`degreeToTier()`](/docs/utils/degree-to-tier) 写进属性，本 composable 只决定「当前该显示到哪一档」：

::component-example
---
name: UseSigmaLabelTiersExample
options:
  - name: breakpoints
    label: breakpoints
    items: [2.6/1.3, 1.8/1.1]
    default: 2.6/1.3
---
::

```ts
const { tier } = useSigmaLabelTiers({ breakpoints: [[2.6, 0], [1.3, 1]] })
```

### `breakpoints`

`[ratio, tier]` 的列表，按 `ratio` 降序，`ratio` 越大视野越广。上面那组读作：比例超过 2.6 只留 0 档，超过 1.3 留到 1 档，再近就不限制。自动 fit 之后比例通常落在 1 附近，默认值因此从 1.3 起跳。

### `attribute`

读取节点档位的属性名，需与 styles 里消费该档位的属性名一致。`degreeToTier()` 默认就写在 `labelTier` 上。

## API

### useSigmaLabelTiers()

`useSigmaLabelTiers(options?: UseSigmaLabelTiersOptions): UseSigmaLabelTiersReturn`{lang="ts-type"}

必须在 `SigmaGraph` 子树内调用。作用域销毁时自动解绑相机监听并把档位复位。

#### Options

::field-group
  :::field{name="breakpoints" type="readonly SigmaLabelTierBreakpoint[]"}
  默认 `[[2.6, 0], [1.3, 1]]` —— 相机比例断点 `[ratio, tier]`，按 `ratio` 降序排列。比例大于 `ratio` 时只显示不高于 `tier` 的档位。
  :::

  :::field{name="attribute" type="string"}
  默认 `'labelTier'` —— 读取节点档位的属性名，需与 styles 里消费该档位的属性名一致。
  :::
::

#### Returns

::field-group
  :::field{name="tier" type="Readonly<Ref<number>>"}
  当前允许显示的最低档位。所有断点都未命中时为 `Infinity`，即不限制。
  :::
::

`SigmaLabelTierBreakpoint` 的形态是 `readonly [ratio: number, tier: number]`。类型从根出口取：

```ts
import type { SigmaLabelTierBreakpoint, UseSigmaLabelTiersOptions, UseSigmaLabelTiersReturn } from '@movk/sigma'
```
