---
title: SigmaZoomControl
description: 放大、缩小、复位三个按钮，图标可经具名插槽替换，行为与无障碍结构由库提供。
category: controls
seo:
  title: SigmaZoomControl
  description: Zoom in, zoom out and reset buttons with replaceable icons, keeping the camera behavior and accessible markup provided by the library.
---

## 用法

放进 `SigmaControls` 即可，内部走 [`useSigmaCamera()`](/docs/composables/use-sigma-camera)。

### `factor`

放大与缩小每次按 `factor` 倍缩放。复位**不吃这个 prop**——它调的是相机的 `reset()`，回到初始视角这件事没有倍数可言：

::component-example
---
name: ZoomControlFactorExample
options:
  - name: factor
    label: factor
    type: number
    default: 1.5
---
::

::note
需要「容纳全图」而不是「回到初始视角」，用 `useSigmaCamera().fitTo()` 自己接一个按钮，那需要可选 peer `@sigma/utils`。
::

### `duration`

三个按钮的动画时长，单位毫秒。

::component-example
---
name: ZoomControlDurationExample
options:
  - name: duration
    label: duration
    type: number
    default: 200
---
::

### `reset`

复位按钮的显隐。`false` 时该按钮整个不渲染，而不是置灰：

::component-example
---
name: ZoomControlResetExample
options:
  - name: reset
    label: reset
    items: [true, false]
    default: true
---
::

### `labels`

三项依次对应放大、缩小、复位，同时用作 `aria-label` 与 `title`，悬浮按钮即可看到。

::component-example
---
name: ZoomControlLabelsExample
options:
  - name: labels
    label: labels
    default: ['Zoom in', 'Zoom out', 'Reset']
---
::

::note
`reset` 为 `false` 时复位按钮不渲染，`labels` 的第三项随之无效。
::

## 示例

### 图标插槽

三个按钮各有具名插槽——`zoom-in`、`zoom-out`、`reset`。它们都没有作用域，**只负责图标**：点击行为、`type="button"`、`aria-label`、`title` 都由库提供，接管外观不会丢掉无障碍结构。

:component-example{name="ZoomControlSlotExample"}

默认图标是 `+`、`−`（U+2212 减号，不是连字符）、`⌖`；`reset` 插槽仅在 `reset` 为 `true` 时渲染。

## API

### Props

:component-props{slug="SigmaZoomControl"}

### Slots

:component-slots{slug="SigmaZoomControl"}
