---
title: SigmaGraph
description: 根组件。创建 Sigma 实例与 graphology 图，下发上下文，透传配置，转发全部 sigma 事件。
category: core
seo:
  title: SigmaGraph
  description: Root component that owns the Sigma instance and graphology graph, provides context to children, passes settings through and forwards every sigma event.
---

## 用法

`SigmaGraph` 在客户端创建 Sigma 实例与 graphology 图，把上下文下发给子树。所有 composable 都要在它的默认插槽内调用。

数据有两条互斥通道：传 `data` 由库经 [`applyGraphDiff`](/docs/utils/apply-graph-diff) 增量同步，传 `graph` 则库完全不碰数据，只负责渲染与生命周期。

:component-example{name="GraphBasicExample" client-only="true" class="example-stage"}

::note
组件本身不带高度。画布用 `height: 100%` 占满容器，所以外层必须给出确定的高度，否则整张图不可见。
::

### 自带 graph 实例

传 `graph` 时库完全不碰数据，只负责渲染与生命周期。图可以用 graphology 生态的任何方式操作，sigma 靠图事件自动重绘：

:component-example{name="GraphExternalExample" client-only="true" class="example-stage"}

这条通道下 `data` 与 `diffOptions` 都不生效。适合数据本来就由别处（Pinia、自己的 diff 逻辑、上游算法库）掌控的场景。

::warning
Vue 会把 props 包成响应式代理。组件内部已经 `toRaw()` 剥回原对象再交给 sigma，但你自己往下传实例时也要注意——把代理对象喂给 graphology 或 sigma 的原生 API 会出问题。
::

### settings 全透传

`settings` 整体交给 sigma，不逐字段枚举也不过滤未知键。v4 的 `Settings` 只管行为与性能，视觉规则在 `styles` 里。下面这个示例传入一个库不认识的配置键，再从 `sigma.getSettings()` 原样读回：

:component-example{name="GraphSettingsExample" client-only="true" class="example-stage"}

模块选项里的全局 `settings` 与组件级 `settings` 深度合并后一起透传，组件级优先。

### 事件

`emits` 覆盖 sigma 事件全集，node / edge / stage 三组直接可监听，payload 类型与上游一致：

:component-example{name="GraphEventsExample" client-only="true" class="example-stage"}

::warning
边事件需要先在 `settings` 里开 `enableEdgeEvents: true`，这是 sigma 的默认行为，不是本库的限制。
::

### styles 与 primitives

v4 把渲染拆成两层：`primitives` 声明「有哪些形状、路径、端点可用」，`styles` 用声明式规则决定「每个节点与边用哪个、什么颜色、多大」。两者都整体透传，不做键白名单。

:component-example{name="GraphPrimitivesExample" client-only="true" class="example-stage"}

::warning
`styles` 与 `primitives` 只在构造实例时读取，sigma 没有提供替换它们的方法。改这两个 prop 会**重建整个实例**；只是想随交互改外观的话，把规则写成读取状态的条件（`whenState`）或函数，再调 `useSigmaReducer().refresh()`。
::

取用 sigma 内置的形状与层工厂时必须经 `defineSigmaPrimitives()` 延迟加载——`sigma/rendering` 与 `sigma/primitives` 在模块顶层就读 WebGL 全局。本库的 `sdfPolygon()` / `sdfStar()` 是纯数据，可直接写在外层。用法见 [`defineSigmaPrimitives`](/docs/utils/define-sigma-primitives)。

### 节点标签的字形图集

节点标签走 SDF 渲染：字形先按某个**源字号**烘进一张 2048×2048 的图集纹理，再按 `styles` 里的 `labelSize` 缩放绘制。源字号由 `labelAtlasFontSize` 决定，默认 `64`，与边标签的图集一致。

sigma 上游给节点标签用的是 `64 × devicePixelRatio`。2 倍屏上单个字形连同 buffer 占约 144px，一页只装得下约 190 个，中文字形集轻易超过——溢出后游标翻到第二页，而上游只上传第一页，翻页那一步还会把第一页截成 1px 宽。症状是**节点标签整体消失**：边标签因字形少仍正常，1 倍屏也正常，控制台没有任何报错。本库因此把默认值压回 64，一页可容约 600 个字形。

::note
SDF 是矢量距离场，源字号减半对 12~24px 的显示字号没有可见影响。Latin 字形集本来就不会溢出，想在很大的 `labelSize` 下更锐利，把 `labelAtlasFontSize` 调回 `128` 即可。
::

### 自带 reducer

`nodeReducer` / `edgeReducer` 作为 reducer 链的基座执行，库内注册的归约在其后叠加，不会被吞掉。签名与 sigma v4 一致：`(key, data, attributes, state, graphState, graph)`。

### 多实例

一页里可以放多张图。给每个实例一个 `id`，就能在组件树之外经 `useSigmaById(id)` 拿到它——用法与示例见 [`useSigmaById`](/docs/composables/use-sigma-by-id)。

::warning
每个 sigma 实例占 3 个 WebGL 上下文，浏览器上限多为 16 个。同屏超过五张图时最早的上下文会被强制丢弃、画布直接变空白，只在控制台留一行 `Too many active WebGL contexts`。列表页请做视口内懒挂载。
::

### 插槽内的定位

默认插槽排在占满高度的画布之后，走正常文档流。插槽内的任何面板都必须自行 `position: absolute`，否则会被挤到容器之外并被 `overflow` 裁掉。

```vue
<template>
  <SigmaGraph :data="data" style="height: 70vh">
    <div class="panel">…</div>
  </SigmaGraph>
</template>

<style scoped>
.panel {
  position: absolute;
  inset: 12px auto auto 12px;
}
</style>
```

## API

### Props

:component-props{slug="SigmaGraph"}

### Emits

:component-emits{slug="SigmaGraph"}

### Slots

:component-slots{slug="SigmaGraph"}

### Expose

经 [`useTemplateRef`](https://vuejs.org/api/composition-api-helpers.html#usetemplateref) 访问该组件实例。

::field-group
  :::field{name="sigma" type="ShallowRef<Sigma | null>"}
  原生 sigma 实例。SSR 期与挂载完成前为 `null`。
  :::

  :::field{name="graph" type="ShallowRef<Graph>"}
  原生 graphology 实例。
  :::
::

::tip
子树内更常用的是 `useSigma()`，它拿到的是同一对实例，不必往上传 ref。
::
