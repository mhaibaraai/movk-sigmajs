---
title: SigmaControls
description: 控件容器，负责四角停靠与排布方向，替使用方做掉绝对定位这件事。
category: controls
seo:
  title: SigmaControls
  description: A container that docks built-in controls to a corner of the canvas and lays them out, handling absolute positioning for you.
---

## 用法

`SigmaGraph` 的默认插槽排在占满高度的画布之后，走的是正常文档流——直接往里塞控件会被挤到容器之外并被 `overflow` 裁掉。`SigmaControls` 就是替你把绝对定位这件事做掉的容器：

:component-example{name="ControlsPositionExample" client-only="true" class="example-stage"}

一张图上可以放多个容器，各停各的角：

```vue
<template>
  <SigmaGraph :data="data" style="height: 70vh">
    <SigmaControls position="top-right">
      <SigmaSearchControl />
      <SigmaZoomControl />
    </SigmaControls>

    <SigmaControls position="bottom-left">
      <SigmaMiniMap />
    </SigmaControls>
  </SigmaGraph>
</template>
```

### 纯布局壳

组件完全不碰 sigma 上下文，只渲染一个带 `data-position` / `data-direction` 的 `div`，停靠与排布全由 CSS 消费这两个属性。因此它也能装任何自定义内容，不限于内置控件。

停靠位置还会影响子控件的行为：`data-position` 以 `bottom` 开头时，`SigmaSearchControl` 的结果列表改为**向上**展开，避免溢出画布底边。

### 交叉轴不拉伸

容器是 flex，但刻意禁用了交叉轴拉伸——否则一个窄按钮会被最宽的兄弟撑开。`direction="vertical"` 且停靠在右侧时右对齐，`horizontal` 时居中对齐。

::note
控件之间的间距由 `--sigma-control-gap` 控制，与画布边缘的距离由 `--sigma-control-inset` 控制，层级是 `--sigma-control-z`（默认 `5`，低于覆盖层的 `10`）。完整变量表见[主题定制](/docs/guides/theming)。
::

## API

### Props

:component-props{slug="SigmaControls"}

### Slots

:component-slots{slug="SigmaControls"}

默认插槽没有作用域，放什么都可以。
