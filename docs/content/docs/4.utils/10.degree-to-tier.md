---
title: degreeToTier
description: 按度数排名给节点分标签档位，返回节点 key 到档位的映射表，档位越小越重要。
seo:
  title: degreeToTier
  description: Rank nodes by degree into label tiers and return a node key to tier lookup, driving label size and display priority.
---

## 用法

一张图该出多少标签由屏幕空间决定，与度数的绝对大小无关。所以这里要的是**排名**，不是取值：

:component-example{name="DegreeToTierExample" client-only="true" class="example-stage"}

```ts
const tiers = degreeToTier(graph)
for (const [node, tier] of Object.entries(tiers)) {
  graph.setNodeAttribute(node, 'labelTier', tier)
}
```

档位有两个去处，都由调用方决定要不要用：

- **字号与配色**：交给 styles 的 `dict` 绑定，档位越低越粗越深。
- **显示优先级**：`labelVisibility: 'visible'` 让枢纽节点不受 sigma 标签网格去重的影响，其余档位留 `'auto'` 参与竞争。

再配合 [`useSigmaLabelTiers()`](/docs/composables/use-sigma-label-tiers)，视野拉远时就能按档位逐级收掉标签。

### 排名而非取值

节点尺寸这类连续量直接用 styles 的数值绑定即可（`{ attribute: 'degree', min, max, minValue, maxValue }`），答的是「这个节点该画多大」；`degreeToTier()` 把度数压成两三个离散的档，答的是「这个节点的标签该不该出、出多大」。前者要平滑，后者要能一刀切。

### ratios 是累计占比

默认 `[0.15, 0.5]` 读作：排名前 15% 是 0 档，前 50% 是 1 档，其余是 2 档。档位数量恒为 `ratios.length + 1`：

```ts
degreeToTier(graph, { ratios: [0.2] })       // 只分两档
degreeToTier(graph, { ratios: [0.1, 0.3, 0.7] }) // 分四档
```

首档向上取整，保证再小的图也至少有一个节点落进最高档。后续各档取四舍五入并保持单调，比例写反或写重也不会让后一档反超前一档。

### 同度数怎么排

默认按节点 key 的字典序排定，同一份数据每次结果一致。要引入别的重要度信号用 `rank`：

```ts
degreeToTier(graph, {
  rank: (g, node) => (g.getNodeAttribute(node, 'pagerank') as number) ?? 0
})
```

::note
只在**度数相同**时才比 `rank`。单用 pagerank 排序往往不合适：它已归一化到 0..1 且度数最高的节点恒为 1，服务端新建的节点还可能整个字段都缺。
::

## API

### degreeToTier()

`degreeToTier(graph: Graph, options?: DegreeToTierOptions): Record<string, number>`{lang="ts-type"}

#### Parameters

::field-group
  :::field{name="graph" type="Graph" required}
  源 graphology 实例，只读不改。
  :::

  :::field{name="options" type="DegreeToTierOptions"}

    ::::collapsible
      :::::field-group
        ::::::field{name="ratios" type="readonly number[]"}
        默认 `[0.15, 0.5]` —— 各档位的累计占比，升序。首档向上取整。
        ::::::

        ::::::field{name="rank" type="(graph: Graph, node: string) => number"}
        度数相同时的次级排序依据，返回值越大排名越靠前。缺省按 key 的字典序排定。
        ::::::
      :::::
    ::::
  :::
::

#### Returns

`Record<string, number>`，节点 key 到档位的映射表，取值范围 `0` 到 `ratios.length`。空图返回 `{}`。

```ts
import type { DegreeToTierOptions } from '@movk/sigma'
```
